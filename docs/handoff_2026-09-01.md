# Handoff 2026-09-01 (planning session closing; peer session owns execution)

Written by the planning session at ~20:55 UTC. The peer session ("active-opd-28")
runs everything from here. Decisions: docs/decisions.md. Vocabulary: CONTEXT.md.
Experiment definition: docs/adr/0005-correctness-bucket-experiment.md.
Loop design findings: docs/design_review.md.

## State of master (HEAD 815be6f, NOT yet pushed: user runs `git push origin master`)
- apod/bank.py + conf/bank.yaml + tests/test_bank.py (question bank; `python -m apod.bank`).
- apod/stages/train.py: per-step diagnostics (overlap_ratio_top16, overlap_adv_top16,
  abs_entropy_gap, response_tokens, cap_hit_frac, bf16_rounded_frac + per-block),
  continued LR schedule (train.total_training_steps + `--global-step-offset N`,
  warmup_steps as count or fraction), atomic checkpoint writes, persist_optimizer.
- apod/tracking.py + conf/tracking.yaml (W&B, mode online; machine logged in as krrish-agar,
  project apod). Run name `<run_dir>/<arm>`, group = run dir.
- apod/stages/rollout_eval.py: `max_num_seqs=target_concurrent_sequences` pass-through.
- Tests (CPU, `PYTHONPATH=$PWD .venv/bin/python tests/<file>`): test_persist_optimizer (7),
  test_train_diagnostics, test_tracking, test_bank, test_rollout_eval_merged, test_grade_aime.
- .venv is the only environment: fla 0.5.2 (fused GDN kernels), vllm 0.26.0, wandb 0.29.0.

## Pending: the driver branch (agent may be dead when you read this)
Branch `worktree-agent-a38e5c2c55d09e575`, worktree
`.claude/worktrees/agent-a38e5c2c55d09e575`, last commit 21467fb
("driver: questions from apod.bank, one rollout_eval session per refresh"; earlier:
07471ec step-based loop apod.driver + selection rules + refresh plots, 0a4ca8a several
eval sets per engine session + max_num_seqs above 256, 0d615ea merge of master).
It has NOT merged the diagnostics/tracking/continued-LR commits (06b17a7) nor the
rollout_eval max_num_seqs line (expect a small conflict in rollout_eval.py: keep ONE
pass-through). To finish:
1. `git merge --no-ff worktree-agent-a38e5c2c55d09e575` on master; resolve; run all tests.
2. Check apod/driver.py against the contract: passes `--global-step-offset` = steps done
   per arm on every train launch (incl. resume), sets train.total_training_steps=100,
   warmup 5 (or 0.05), lr 3.1623e-06, persist_optimizer true, calls
   tracking.init/log_refresh/finish, AIME (aime2526, 16 samples) inside the same engine
   session as MATH-500 + rollouts, questions from apod.bank (bucket_questions / entropy sort),
   run dirs r1-correctness-8k / r2-trajsel-8k / r3-qentropy-8k, per-refresh plots.
3. Anything missing: implement it there, small and plain (docs/decisions.md style).

## Probes (running as one chain, survive session close)
Chain: scratchpad `bench/run_probes.sh` (path in `ps -ef | grep run_probes`), ~21:50 UTC end.
Outputs (scratchpad bench/*.jsonl; also copy to docs/perf_review.md when read):
- batch_probe_results.jsonl: micro 16 then 8, cap 8192, kernels on. Adopt the largest micro
  that survived with peak_mib < 75000 -> conf/train/gkd.yaml per_device_train_batch_size,
  gradient_accumulation_steps = 32 // (2*micro). Note s_per_step next to the old 71.5 GiB comment.
- conc_probe_results.jsonl (2B) and conc_probe_teacher (9B): 256 vs 512. Adopt 512 in
  conf/engine/default.yaml only if req_per_min >= 1.8x of 256 AND no "preempt" mentions in the
  eval.log; otherwise stay at 256. Run dirs outputs/runs/kl50_batchprobe, kl50_conc*, are
  disposable (delete after reading).

## Validation before any real launch (GPU, small)
a. Bank smoke: `python -m apod.bank --gpus 0,1 bank.student_questions=<small> bank.target_per_bucket=<small>`
   with a disposable bank dir; `--report`; rerun must generate nothing (resume).
b. Driver refresh smoke (r1 shape): 2 steps/pass, few eval problems, throwaway run dir; check in
   W&B: all per-step keys present; learning_rate on pass-2 step 1 continues (not 0/peak);
   optimizer_state.pt saved each pass and "optimizer state restored from" logged on the next;
   kill mid-run and resume lands on the right global step and LR; peak train memory < 80 GiB
   with diagnostics on (else train.diag_chunk 512).

## Launch order (sequential, both GPUs per arm)
bank-8k (14,500 student questions, 800/bucket, ~17 h) -> r1-correctness-8k (4 bucket arms x 100
steps) -> r2-trajsel-8k (entropy_top4, kl_high, kl_mid, kl_low, random; 12 rollouts keep 4;
exact reverse KL) -> r3-qentropy-8k (student H(q) top vs random questions; 4 rollouts).
Common: cap 8192, strict scoring, effective batch 32, refresh every 10 steps with MATH-500
avg@4/pass@4 + AIME 2025+2026 avg@16/pass@16, per-step diagnostics, persist_optimizer,
one LR schedule per run (warmup 5, cosine to 0.1 at step 100), peak lr 3.1623e-06,
sampling T=1.0 top_p 0.95 top_k 20 no penalty. No "all" arm, no entropy_mid, no diversity arm.

## Repo hygiene
Commit each config value with its reason; push after each; one dated line per decision in
docs/decisions.md. If the plan is impossible as specified, record the question in
decisions.md and stop; do not silently change the design.
